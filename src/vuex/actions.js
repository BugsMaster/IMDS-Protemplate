import api from 'assets/js/api';

const actions = {
    // getUserTypeMenu({ commit }) {
    //     api.getUserTypeMenu().then(data => {
    //         commit('getUserTypeMenu', data);
    //     });
    // },
    // getEquipmentsList({ commit }) {
    //     api.getAllEquipModules().then(({data}) => {
    //         data.forEach((element) => {
    //             element.value = element.type;
    //             element.label = element.type;
    //             element.children.forEach((item) => {
    //                 item.value = item.eqp_id;
    //                 item.label = item.eqp_id;
    //             })
    //         });
    //         commit('getEquipmentsList', data);
    //     });
    // },
};

export default actions
